# SOPROFEN Production Query Editor - Frontend Configuration

# Mock mode is the default while no backend exists. Flip to false and set VITE_API_URL
# once a backend is in place.
VITE_USE_MOCK=true
# VITE_API_URL=http://localhost:3001/api
